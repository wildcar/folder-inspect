# folder-inspect — опросник для уточнения задачи и выбора стека

> Документ обращён к владельцу проекта, поэтому написан по-русски.
> Для каждого вопроса указан **вариант по умолчанию** — он принимается, если ответа нет.
> Ответы зафиксированы в `AGENTS/SPEC.md` (контракт v0.1); решение по стеку — `docs/adr/0001-stack-and-interface.md`.

Статус: ✅ закрыт 2026-09-15 — все ответы получены, контракт в `AGENTS/SPEC.md` v0.2. Дата создания: 2026-09-15.

---

## A. Уточнение задачи

Исходная формулировка: «проверка хранилища файлов на наличие очень больших файлов,
архивов, мусорных файлов, дублирующихся файлов; цель — почистить репозитории от ненужных файлов».

### A1. Что именно сканируем
- (a) обычные папки на локальном диске (проекты, документы, загрузки)
- (b) git-репозитории — рабочие копии
- (c) git-репозитории, включая историю
- (d) сетевые шары / NAS / внешние диски
- (e) несколько корней за один запуск

**Ответ (2026-09-15):** любые папки на диске. Это **проектные репозитории** — хранилища документов по
проектам внедрения (код там редко). С git не связано вообще; на git не ориентироваться и не завязываться.
Несколько корней и сетевые пути — поддерживаем как обычные папки.

### A2. Объёмы
**Ответ:** десятки тысяч файлов.

### A3. Целевые ОС
**Ответ:** Windows + Linux.

### A4. Кто пользователь
**Ответ:** не уточнялось; принято умолчание — владелец + коллеги (не обязательно разработчики), отсюда
требование «нормального интерфейса» в A11.

### A5. Режим использования
**Ответ:** вручную, по необходимости.

### A6. «Очень большие файлы»
**Ответ:** нужны **градации по типу файла**, порог для каждой настраивается:
- вообще большие файлы (записи видеовстреч, дистрибутивы) — больше 100 МБ;
- файлы, которые не должны быть большими: doc/ppt больше 15 МБ, картинки больше 5 МБ.

**Уточнение (2026-09-15):** xls/xlsx — 15 МБ, PDF — 30 МБ. Топ-N самых больших файлов и папок показываем всегда.

### A7. Архивы
**Ответ:** архив определяется **по расширению**. Проектный репозиторий должен содержать файлы в прямом
доступе — открывающиеся по ссылке. Архив — признак дистрибутива, попытки сохранить старые версии или
объединить файлы для отправки; их быть не должно. docx и подобные контейнеры — не архивы.

**Уточнение (2026-09-15):** да, **дистрибутивы/установщики** (exe, msi, deb, rpm, dmg, pkg) — отдельная
категория рядом с архивами, включена по умолчанию.

### A8. «Мусорные» файлы
**Ответ:** bak, .DS_Store, Thumbs.db и подобное. Принятый список по умолчанию: `*.bak *.tmp *.temp *.old
*.orig *.swp`, файлы блокировок Office `~$*`, `Thumbs.db`, `desktop.ini`, `.DS_Store`, `._*`,
недокачанные `*.crdownload *.part`; плюс пустые папки и файлы нулевого размера. Список расширяется в
конфиге. Учёт `.gitignore` — снят (нет git).

### A9. Дубликаты
**Ответ:** точные дубликаты — по содержимому. Похожие имена — надо подумать; есть паттерн
«Копия … .docx». Принято: похожие имена показываем **отдельно, как кандидатов**, без автоматических
действий. Стартовый список паттернов: `Копия <имя>`, `<имя> - копия`, `<имя> - копия (N)`, `Copy of <имя>`,
`<имя> (N)`, `<имя>_v2/_final/_старый/_old`, `<имя> (Восстановлен)`.

**Уточнение (2026-09-15):** «главный» файл в группе дубликатов выбирается **вручную в интерфейсе**;
интерфейс подсказывает самый старый по дате изменения. Список паттернов похожих имён дополняется по ходу.

### A10. Действия над найденным
**Ответ:** отчёт **и чистка**: карантинная папка с возможностью откатить + вместо удалённого дубликата
остаётся файл-указатель. Прямого удаления нет; по умолчанию инструмент ничего не меняет, любые действия —
через план и `apply`.

**Уточнение (2026-09-15):** hardlink **не нужен**. Вместо удалённого дубликата остаётся **файл-ссылка**
(`<имя файла>.duplicate.txt`), который показывает, что дубликат удалён, и где лежит оригинал —
**относительным путём**. Сам дубликат уезжает в карантин. Карантин — `<корень>/.folder-inspect/quarantine`.

### A11. Формат отчёта
**Ответ:** нужен нормальный интерфейс и возможность выгрузки.

Принято (см. ADR-0001): результат сканирования сохраняется в файл `report.json` — это и есть «JSON-вывод»:
машиночитаемый снимок всех находок, из которого строятся все представления. Пользователю его читать не нужно.
Поверх него: краткая сводка в консоли, **веб-интерфейс на localhost** (открывается в браузере, встроен в
тот же exe, без установки и без интернета) с фильтрами, группами дубликатов и галочками для плана действий,
и выгрузка в **CSV, XLSX, HTML**.

**Уточнение (2026-09-15):** интерфейс в браузере подтверждён.

### A12. Исключения и безопасность
**Ответ:** не уточнялось; принято умолчание — исключения через конфиг `.folder-inspect.yml` в корне и флаги;
по симлинкам/junction не ходим; системные папки и собственную карантинную папку не трогаем.

---

## B. Платформа, язык, интерфейс

### B1. Интерфейс
**Ответ:** умолчание подходит — CLI-ядро; поверх него веб-интерфейс (см. A11).

### B2. Язык / рантайм
**Ответ:** Go.

### B3. Дистрибуция
**Ответ:** не уточнялось; принято умолчание — один исполняемый файл на ОС, GitHub Releases.

### B4. Зависимости
**Ответ:** не уточнялось; принято умолчание — сторонние модули минимально и только проверенные
(YAML-конфиг, XLSX-экспорт).

### B5. Производительность
**Ответ:** не уточнялось; для десятков тысяч файлов достаточно параллельного обхода и хеширования только
кандидатов в дубликаты. Кеш между запусками не нужен.

### B6. Конфигурация
**Ответ:** не уточнялось; принято умолчание — YAML-файл + флаги, флаги переопределяют.

### B7. Локализация
**Ответ:** не уточнялось; принято умолчание — русский + английский, выбор флагом, автоопределение по ОС.

### B8. Тестирование
**Ответ:** не уточнялось; принято умолчание — unit-тесты детекторов + интеграционные на сгенерированной
«грязной» папке; генератор фикстуры используется и для демо.

### B9. Лицензия и публичность
**Ответ (2026-09-15):** MIT. Репозиторий на GitHub публичный.

---

## C. Приоритеты первой версии (MVP)

Ответа не было; предлагаемый порядок, исходя из ответов выше:

| Функция | Приоритет |
|---|---|
| Сканирование, конфиг, `report.json`, сводка в консоли | 1 |
| Большие файлы с градациями по типу, топ-N | 1 |
| Архивы и дистрибутивы | 1 |
| Мусор по встроенным правилам + свои паттерны | 1 |
| Пустые папки / нулевые файлы | 1 |
| Точные дубликаты | 1 |
| Веб-интерфейс на localhost с планом действий | 1 |
| Выгрузка CSV / XLSX / HTML | 1 |
| Карантин + восстановление | 1 |
| Удаление дубликатов в карантин с файлом-указателем на оригинал | 1 |
| Похожие имена (кандидаты) | 2 |
| Русский + английский интерфейс | 2 |
| История сканов и сравнение | 3 |
| Всё, что связано с git | не нужно |
